machine:
  python:
    version: 2.7.11

dependencies:
  pre:
    - echo "None, for now"
    
test:
  override:
    - echo "integrationtest-report-sequential.xml" > $CIRCLE_ARTIFACTS/integrationtest-report-sequential.xml
