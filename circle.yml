version: 2
executorType: docker
containerInfo:
  - image: lots0logs/mariadb-alpine
  - image: lots0logs/nginx-fpm-alpine
  - image: node:alpine
  - image: ruby:alpine
stages:
  build:
    workDir: /opt
    steps:
      - type: shell
        command: return 0
