version: 2
jobs:
  build:
    docker:
      - image: circleci/ruby:2.3.4
    steps:
      - run: sudo apt install dig
      - run: 
          command: dig +trace package.elm-lang.org
          shell: bash --login -eo pipefail
