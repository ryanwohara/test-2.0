version: 2
executorType: docker
containerInfo:
  - image: node:alpine
  - image: ruby:alpine
stages:
  build:
    workDir: /
    steps:
      - type: shell
        command: |
          touch file
          setfacl -m u:root:r file
          return 0
