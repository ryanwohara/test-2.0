version: 2
executorType: docker
containerInfo:
  - image: openjdk:8
stages:
  build:
    workdir: /project
    steps:
    - type: shell
      pwd: /
      command: |
        apt update && apt install -y mtr sudo
        mtr -r google.com
        sudo mtr -r google.com
