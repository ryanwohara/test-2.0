version: 2
executorType: docker
containerInfo:
  - image: node:alpine
stages:
  build:
    workDir: /home/circlei/project
    steps:
      - type: shell
        shell: /bin/sh
        pwd: /
        command: apk -U add git
      - type: checkout
      - type: shell
        shell: /bin/sh
        command: return 0
