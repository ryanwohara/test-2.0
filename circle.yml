version: 2
executorType: docker
containerInfo:
  - image: rohara/elixir:0.1
stages:
  build:
    workDir: /home/circleci/project
    steps:
      - type: shell
        shell: /bin/sh
        pwd: /
        command: |
          ls -laR /
          netstat -plantu || ss -plntu
          ps auxfww
