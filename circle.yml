dependencies:
  override:
    - echo $PATH
    - curl -I https://gradle.org/ || true

test:
  override:
    - sudo apt-get update
    - sudo apt-get install -y ca-certificates
    - sudo update-ca-certificates -f
    - curl -I https://gradle.org/ || true

