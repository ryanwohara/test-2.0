machine:
  node:
    version: 5.1.0
    
dependencies:
  pre:
    - echo "None, for now"
    
test:
  override:
    - nvm use 5.0.0; node --version; npm --version
    - nvm use 5.1.0; node --version; npm --version
  post:
    - mkdir $CIRCLE_ARTIFACTS/build
    - echo "moo!" > "$CIRCLE_ARTIFACTS/build/onboarding.test.js.ProjectBuildOnboardingFlow when currentStep is 2 with country selected and agreed terms matches image.0.html"
