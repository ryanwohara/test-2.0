version: 2
jobs:
  build:
    machine: true
    shell: sudo /bin/bash
    steps:
      - checkout
      - run: ls -la /var/run/docker.sock
      - run: docker ps -a
      - run: docker images
      - run: ps auxfww
      - run: docker build -t foo .
      - run: df -T

workflows:
  version: 2
  test_and_deploy:
    jobs:
      - build
