version: 2
executorType: machine

stages:
  build:
    workDir: ~/project
    steps:
      - type: checkout
      - type: shell
        command: sudo /bin/bash -c "apt-get update && apt-get install php-cli"
      - type: shell
        command: php --version
